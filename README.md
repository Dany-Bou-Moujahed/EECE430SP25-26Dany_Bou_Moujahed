#HW 4 and 5
Docker image ran locally, and also via codespaces on https://ubiquitous-enigma-qvq6x5rxjv29jr6-8000.app.github.dev/

also i was bored so i setup github actions so the docker image is built on any push to main, so now the image can also be found on the packages tab of the repo.
